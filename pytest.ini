[pytest]
pythonpath = .
testpaths = tests/unit tests/integration
asyncio_mode = auto
markers =
    integration: tests that use the shared development database
    live: real listing-site scrapes; run with pytest tests/live
